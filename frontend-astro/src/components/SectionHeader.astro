---
// Интерфейс пропсов компонента
interface Props {
  title: string
  subtitle?: string
}

// Деструктуризация пропсов для удобного использования в шаблоне
const { title, subtitle } = Astro.props
---

<!--ЗАГОЛОВОК СЕКЦИИ -->
<div class="section-header">
  <!-- ОСНОВНОЙ ЗАГОЛОВОК  -->
  <h2 class="section-header__title">{title}</h2>
  <!--ПОДЗАГОЛОВОК  -->
  {subtitle && <p class="section-header__subtitle">{subtitle}</p>}
</div>

<style lang="scss">
.section-header {
  display: flex;
  align-items: center;
  gap: 24px;
  margin-bottom: 80px;

  /* ОСНОВНОЙ ЗАГОЛОВОК (h2)*/
  &__title {
    font-family: 'Montserrat Alternates', sans-serif;
    font-size: 30px;
    font-weight: 500;
    background-color: var(--color-primary);
    color: #fff;
    padding: 8px 20px;
    border-radius: 12px;
    white-space: nowrap;
  }

  /* ПОДЗАГОЛОВОК */
  &__subtitle {
    font-size: 16px;
    line-height: 1.4;
    color: var(--color-text);
    max-width: 390px;
  }
}

@media (max-width: 768px) {
  .section-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
    margin-bottom: 24px;

    &__title {
      font-size: 22px;
      padding: 6px 16px;
    }

    &__subtitle {
      font-size: 14px;
      max-width: 100%;
    }
  }
}
</style>
